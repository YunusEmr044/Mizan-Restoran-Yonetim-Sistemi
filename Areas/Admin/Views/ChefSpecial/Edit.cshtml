@model ChefSpecial
@{
    ViewData["Title"] = "Şefin Önerisini Düzenle";
    var menuItems = (List<MenuItem>)ViewBag.MenuItems;
}
<h2 class="mb-4">Şefin Önerisini Düzenle</h2>
<form asp-action="Edit" asp-route-id="@Model.Id" method="post" enctype="multipart/form-data">
    <div asp-validation-summary="All" class="text-danger"></div>
    <input type="hidden" asp-for="Id" />
    <div class="mb-3">
        <label asp-for="MenuItemId" class="form-label"></label>
        <select asp-for="MenuItemId" class="form-select">
            @foreach (var m in menuItems)
            {
                <option value="@m.Id">@m.Name (@m.Price.ToString("0.00") ₺)</option>
            }
        </select>
    </div>
    <div class="alert alert-info small">Aşağıdaki alanları boş bırakırsanız ürünün kendi bilgileri kullanılır.</div>
    <div class="mb-3">
        <label asp-for="TitleOverride" class="form-label"></label>
        <input asp-for="TitleOverride" class="form-control" />
    </div>
    <div class="mb-3">
        <label asp-for="DescriptionOverride" class="form-label"></label>
        <textarea asp-for="DescriptionOverride" class="form-control" rows="2"></textarea>
    </div>
    <div class="row">
        <div class="col-md mb-3">
            <label class="form-label">Görsel - değiştirmek için yeni dosya seçin</label>
            @if (!string.IsNullOrEmpty(Model.ImageUrlOverride))
            {
                <div class="mb-2 d-flex align-items-center gap-3">
                    <img src="@Model.ImageUrlOverride" style="max-height:90px;" class="rounded" alt="" />
                    <div class="form-check">
                        <input type="checkbox" name="removeImage" value="true" class="form-check-input" id="removeImage" />
                        <label class="form-check-label" for="removeImage">Görseli kaldır</label>
                    </div>
                </div>
            }
            <input type="file" name="imageFile" class="form-control" accept="image/*" />
        </div>
        <div class="col-md mb-3">
            <label asp-for="PriceOverride" class="form-label"></label>
            <input asp-for="PriceOverride" class="form-control" />
        </div>
    </div>
    <div class="row">
        <div class="col-md mb-3">
            <label asp-for="StartDate" class="form-label"></label>
            <input asp-for="StartDate" type="date" class="form-control" value="@Model.StartDate.ToString("yyyy-MM-dd")" />
        </div>
        <div class="col-md mb-3">
            <label asp-for="EndDate" class="form-label"></label>
            <input asp-for="EndDate" type="date" class="form-control" value="@Model.EndDate.ToString("yyyy-MM-dd")" />
        </div>
    </div>
    <div class="form-check mb-3">
        <input asp-for="IsActive" class="form-check-input" />
        <label asp-for="IsActive" class="form-check-label"></label>
    </div>
    <button type="submit" class="btn btn-primary">Kaydet</button>
    <a asp-action="Index" class="btn btn-outline-secondary">Vazgeç</a>
</form>
